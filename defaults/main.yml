---
rvm_user: "{{ user }}"
rvm_user_home: "{{ host_user_homes }}/{{ rvm_user }}"
rvm_dir: "{{ rvm_user_home }}/.rvm"
rvm_bin: "{{ rvm_dir }}/bin/rvm"
rvm_script: "{{ rvm_dir }}/scripts/rvm"
rvm_default_ruby: false
